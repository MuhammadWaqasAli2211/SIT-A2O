fastapi==0.115.6
# Required by FastAPI to parse multipart bodies — candidate pictures and documents.
python-multipart==0.0.32
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0
pyjwt[crypto]==2.10.1
httpx==0.28.1

# Streams a ZIP out as it is built, rather than requiring a seekable output the
# way stdlib zipfile does. A bulk document export reaches ~3.85 GB at worst
# case, which is not something to hold in memory or stage on disk first.
zipstream-ng==1.8.0
# Renders the onboarding forms to PDF at export time. Chosen over an
# HTML-to-PDF engine (WeasyPrint/wkhtmltopdf) because those need system-level
# native libraries; this is pure Python and installs the same everywhere.
# Pulls in pillow, which it uses to place the base64 photos and signatures
# stored in the form JSON.
reportlab==4.2.5

# QR codes for the candidate ID card. Pure Python, no native dependencies,
# and pillow (already pulled in by reportlab) renders the PNG. The card's
# code encodes plain text only - no URL, no lookup endpoint.
qrcode==8.0

# --- dev ---
pytest==8.3.4

# Test-only, and not imported by the application at all: these read the PDFs
# the ID card service produces, so the card's page size and the QR code are
# asserted against the rendered output rather than against the code that
# claims to have drawn it. Both are pure wheels with no native dependencies.
pypdfium2==5.13.0
zxing-cpp==3.1.1
